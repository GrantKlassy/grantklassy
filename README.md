# grantklassy
`sudo dnf install git gh`

`gh auth login`

`cd && git init && git pull https://github.com/GrantKlassy/grantklassy.git main`

# poetry

1. She "redirect" on my "pipe" until I "output to /dev/null"
